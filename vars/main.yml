miniconda_installer: Miniconda{{ "3" if miniconda_python == 3 else "" }}-{{ miniconda_version }}-{{ miniconda_systems[ansible_system] }}-{{ 'x86_64' if ansible_architecture == 'x86_64' else 'x86' }}.sh
miniconda_systems:
  Linux: Linux
  Darwin: MacOSX
